# Currency-converter
Developed a Python-based Currency Converter that fetches real-time exchange rates using the ExchangeRate-API. The app allows users to convert any amount between two currencies, handling invalid inputs and API errors gracefully. Supports multiple conversions in a single session with a user-friendly console interface.
